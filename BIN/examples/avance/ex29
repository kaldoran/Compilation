% MANQUE DIDEE %

type ideee :
	struct 
		uneidee : char;
	end struct;

type idee : array [ 0 .. 2 , 0 .. 100] of ideee; 
# nbidee idee 
#   0    aucune
var idtodo : int;
type todo : array [ 0 .. 100 ] of ideee;

function estdefinie(id : int) return bool
    start {
        return id;
    };


function add_todo(id : int, uneidee : int) return int 
    var add_todo : todo; 
    start {
	    if( estdefinie(uneidee) and id > 0) then{
		    
		    add_todo[id].uneidee = uneidee;
		    id++;
		    return id;
	    }
	    return 0;
    };

function genere_idee(id : int) return string[50]
	var i, j, a : float;
	var cpt, florian, ronan : int;
	var tmp : string[50];

    
	start {
		for ( cpt = 0; cpt < 100 ; cpt++) do {
		    tmp = "";
		    write("Tu as plein d'idée maintenant");
		}
	};


start { 
;
}
