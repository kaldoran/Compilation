%PROGRAM% 

type type_normal : struct
  test_int : int;
  test_char : char;
end struct;

type type_tableau : array[0 .. 3, 0 .. 4, 0 .. 2] of int;


proc prem_proc(arg : type_tableau, arg_2 : type_normal) 
  var i : int;
  var k : type_tableau;
  
  start {
    while (1 > 2) do {
      i += 1;
    }
  };

var tableau : type_tableau;

proc abcd()
  start {;};

start 
{
  tableau[1][2][0] = 18; /* Pile : 12 */
  tableau[2][1][1] = 10; /* Pile : 19 */
}


