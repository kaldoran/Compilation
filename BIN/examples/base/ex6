%PROGRAM% 

type pepe : struct
  test_int : int;
  test_char : bool;
end struct;

type type_normal : struct
  test_int : int;
  test_char : pepe;
end struct;

type type_tableau : array[1 .. 3] of int;

var variable_carac : char;
var tab : type_tableau;
var var_struct : type_normal;

function prem_fun (arg : int, arg_2 : int) return int
    var i : int;
    var variable_carac : char;
    var tab : type_tableau;
    var var_struct : type_normal;
    var annee : int;
    var c : int;

    start{

        while (1 > 2) do { 
            write ("coucou les amis"); 
        }
    };

function fun (arg1:int) return int
    start{
        write("coucou les amis");
    };

var coucou,hello:char;
var annee: int;
var p :type_normal;
var heu : char; 

start {

  coucou = p.test_char.test_int;
  prem_fun(5, fun(5));
  read(annee, heu);
  write("J'ai %% ans et toutes mes %% !\n", annee, heu);
}

